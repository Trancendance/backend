{
  "name": "transcendence-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run clean && npx tsc -p tsconfig.json && npm run copy-assets",
    "copy-assets": "cp -r src/microservices/gateway dist/microservices/ && cp -r src/microservices/websocket dist/microservices/ && cp -r src/microservices/game dist/microservices/ && cp -r src/public dist/ && cp -r certs dist/",
    "clean": "rm -rf dist && mkdir -p dist",
    "dev": "npx tsx watch src/microservices/api/server.ts",
    "fclean": "rm -rf dist node_modules package-lock.json",
    "verify-build": "npm run build && echo '=== Estructura compilada ===' && find dist -type f -name '*.js' | sort && echo '=== Gateway existe? ===' && ls -la dist/microservices/gateway/ && echo '=== Websocket existe? ===' && ls -la dist/microservices/websocket/"
  },
  "dependencies": {
    "@fastify/static": "^8.0.1",
    "@fastify/websocket": "^11.2.0",
    "better-sqlite3": "^12.2.0",
    "fastify": "^5.6.0",
    "sequelize": "^6.37.7",
    "sqlite3": "^5.1.7",
    "yup": "^1.7.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.8.0",
    "@types/sequelize": "^4.28.20",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
